{
  "name": "adb-claw",
  "version": "2.0.0",
  "description": "Android device control CLI for Gemini 3.8 Flash Computer Use. Perception is a JPEG frame only. Actions use a 0-999 normalized coordinate grid.",
  "capabilities": {
    "input_methods": ["adb_input", "unicode_clipboard"],
    "stealth_levels": ["off"],
    "humanize": false,
    "unicode_input": true,
    "persistent_session": true,
    "image_only": true,
    "normalized_coords": true
  },
  "model": {
    "name": "gemini-3.8-flash",
    "thinking_level": "low",
    "media_resolution": "medium",
    "image": "inline JPEG from frame path, never base64 in tool JSON"
  },
  "tools": [
    {
      "name": "take_screenshot",
      "description": "Read the latest screen JPEG. Never looks at UI text nodes.",
      "usage": "adb-claw observe --quality 60   OR serve frame.latest",
      "returns": "path, frame_seq (serve), device/image size, scale, frame_age_ms",
      "when_to_use": "Before deciding an action. Read the image file. Do not paste JSON."
    },
    {
      "name": "click",
      "description": "Tap using Gemini Computer Use 0-999 coordinates.",
      "usage": "adb-claw tap --normalized X Y   OR serve act {action:tap, frame_seq, x, y}",
      "when_to_use": "After reading the latest frame. Always pass the frame_seq you saw."
    },
    {
      "name": "long_press",
      "description": "Long press using 0-999 coordinates.",
      "usage": "adb-claw long-press --normalized X Y [--duration 1000]",
      "when_to_use": "For context menus or press-and-hold controls."
    },
    {
      "name": "scroll",
      "description": "Scroll the screen. Prefer direction pages; swipe also accepts 0-999 endpoints.",
      "usage": "adb-claw scroll {up|down|left|right} [--pages N]   OR adb-claw swipe --normalized X1 Y1 X2 Y2",
      "when_to_use": "To reveal more content after reading the current frame."
    },
    {
      "name": "type",
      "description": "Type text into the focused field. Unicode uses an embedded app_process clipboard helper; no APK or IME change.",
      "usage": "adb-claw type TEXT",
      "when_to_use": "After clicking an editable field to focus it. Prefer a known deep link when it avoids UI steps."
    },
    {
      "name": "key",
      "description": "Press a system key.",
      "usage": "adb-claw key {HOME|BACK|ENTER|TAB|DEL|POWER|VOLUME_UP|VOLUME_DOWN}",
      "when_to_use": "Back, home, confirm, or device keys."
    },
    {
      "name": "open_app",
      "description": "Launch an app or open a deep link / URL.",
      "usage": "adb-claw app launch PACKAGE   OR adb-claw open URI",
      "when_to_use": "To jump to an app or pass CJK text as a URL parameter."
    },
    {
      "name": "serve",
      "description": "Persistent JSONL session: frame.latest / frame.wait_after / act / device.info.",
      "usage": "adb-claw serve --stdio",
      "when_to_use": "Gemini 3.8 Flash adapters. Image bytes stay on disk; JSON only has path + frame_seq."
    },
    {
      "name": "wait",
      "description": "Wait for an activity or a visual screen change. No text locators.",
      "usage": "adb-claw wait {--activity NAME | --changed} [--timeout ms]",
      "when_to_use": "After navigation, instead of a fixed sleep."
    },
    {
      "name": "bench",
      "description": "Measure first frame, screenshot, latest write, and normalized tap latency.",
      "usage": "adb-claw bench [--rounds 5] [--width px]",
      "when_to_use": "To collect a repeatable image-only latency baseline."
    },
    {
      "name": "app_current",
      "description": "Get the currently focused app and activity.",
      "usage": "adb-claw app current",
      "when_to_use": "To load an App Profile or confirm navigation."
    },
    {
      "name": "device_info",
      "description": "Get device details: model, screen size, Android version.",
      "usage": "adb-claw device info",
      "when_to_use": "At the start of a session."
    }
  ],
  "workflow_hints": {
    "image_only": "The only perception input is a JPEG file from observe or frame.latest. Do not request a UI tree.",
    "normalized_coords": "All model actions use a 1000x1000 (0-999) grid. adb-claw maps them onto the frame's device_width/device_height.",
    "stale_frame": "STALE_FRAME means the screen changed or the frame aged out. Call frame.latest again before acting.",
    "no_sleep": "Never insert sleep, time.sleep, adb-claw shell sleep, or a timed pause between commands. After tap/swipe/open/key/type, immediately observe or frame.latest.",
    "wait_after": "One-shot CLI: observe immediately after an action; only wait --changed if that frame is visibly transitional. Serve: frame.wait_after already returns a JPEG path, so never follow it with frame.latest.",
    "unicode": "Focus the field and call type. Unicode uses adb-claw's embedded app_process clipboard helper. Never install ADBKeyboard, change IME, or call the raw clipboard service.",
    "forbidden_shell": "Never use shell for UI hierarchy extraction, layout/accessibility dumpsys, clipboard service calls, or IME changes. Use JPEG perception, app current, and type.",
    "retry_policy": "Empty/error/placeholder screens are valid outcomes. On the same screen, do not tap the same normalized point more than twice; never switch to raw pixels or node inspection.",
    "media": "Adapter default: gemini-3.8-flash, thinking_level=low, media_resolution=medium, inline JPEG. Upgrade to high only for dense small text.",
    "cjk": "Prefer a known deep link to reduce steps; otherwise focus the field and use built-in Unicode type."
  }
}
